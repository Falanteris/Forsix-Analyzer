version: 2.1

workflows:
  version: 2
  build:
    jobs:
      - build

jobs:
  build:
    working_directory: .
    docker:
      - image: circleci/node:10.16.3
    steps:
      - checkout
      - run:
          name: Perform a quick test
          command: 'npm test'